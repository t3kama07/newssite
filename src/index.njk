---
layout: layout.njk
title: GlobalPulse News - Latest Headlines
description: Stay updated with breaking news and top stories from around the world.
---
<h2>Latest News</h2>
{% if news and news.length %}
  {% for article in news | slice(0,10) %}
    <article>
      <h3><a href="/articles/{{ article.title | slug }}/">{{ article.title }}</a></h3>
      {% if article.image_url %}
        <img src="{{ article.image_url }}" alt="{{ article.title }}">
      {% endif %}
      <p>{{ article.description }}</p>
      <small>{{ article.pubDate }}</small>
    </article>
  {% endfor %}
{% else %}
  <p>No news available at the moment.</p>
{% endif %}

<hr>
<p><a href="/archive/">View Older News</a></p>
