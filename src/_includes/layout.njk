<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  
  <!-- Canonical URL -->
  <link rel="canonical" href="{{ site.url }}{{ page.url }}">
  
  <!-- Open Graph Meta Tags -->
  <meta property="og:title" content="{{ title }}">
  <meta property="og:description" content="{{ description }}">
  {% if image %}
  <meta property="og:image" content="{{ image }}">
  {% endif %}
  <meta property="og:type" content="article">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  
  <!-- Viewport for Mobile -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Basic Styling -->
  <style>
    body { font-family: Arial, sans-serif; max-width: 900px; margin: auto; padding: 15px; }
    header { text-align: center; margin-bottom: 20px; }
    main { line-height: 1.6; }
    img { max-width: 100%; height: auto; margin: 10px 0; }
    footer { text-align: center; margin-top: 40px; font-size: 14px; color: #666; }
    h1, h2, h3 { color: #222; }
    a { color: #0044cc; text-decoration: none; }
    a:hover { text-decoration: underline; }
  </style>

  <!-- Schema.org JSON-LD -->
  {% if page.url contains '/articles/' %}
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "NewsArticle",
    "headline": "{{ title | escape }}",
    "image": ["{{ image }}"],
    "datePublished": "{{ article.pubDate }}",
    "author": {
      "@type": "Organization",
      "name": "Your News Site"
    },
    "publisher": {
      "@type": "Organization",
      "name": "Your News Site",
      "logo": {
        "@type": "ImageObject",
        "url": "{{ site.url }}/logo.png"
      }
    }
  }
  </script>
  {% endif %}
</head>
<body>
<header>
  <h1><a href="{{ site.url }}/">Your News Site</a></h1>
</header>
<main>
  {{ content | safe }}
</main>
<footer>
  <p>&copy; {{ "now" | date }} Your News Site | <a href="/sitemap.xml">Sitemap</a></p>
</footer>
</body>
</html>
