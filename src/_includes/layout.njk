<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="canonical" href="{{ site.url }}{{ page.url }}">

  <meta property="og:title" content="{{ title }}">
  <meta property="og:description" content="{{ description }}">
  {% if image %}
    <meta property="og:image" content="{{ image }}">
  {% endif %}
  <meta property="og:type" content="article">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta name="twitter:card" content="summary_large_image">

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body { font-family: Arial, sans-serif; max-width: 900px; margin: auto; padding: 15px; }
    header { text-align: center; margin-bottom: 20px; }
    main { line-height: 1.6; }
    img { max-width: 100%; height: auto; margin: 10px 0; }
    footer { text-align: center; margin-top: 40px; font-size: 14px; color: #666; }
    h1, h2, h3 { color: #222; }
    a { color: #0044cc; text-decoration: none; }
    a:hover { text-decoration: underline; }
  </style>
</head>
<body>
<header>
  <h1><a href="{{ site.url }}/">GlobalPulse News</a></h1>
</header>
<main>
  {{ content | safe }}
</main>
<footer>
  <p>&copy; {{ "now" | year }} GlobalPulse News | <a href="/sitemap.xml">Sitemap</a></p>
</footer>
</body>
</html>
